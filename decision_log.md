# Decision Log

| Date | Decision | Rationale |
|---|---|---|
| 2026-07-28 | Binary main label (unfair = 1/0), with an `uncertain` flag as a separate field | With ~25-33 reviews per language, a 3-class scheme spreads counts too thin for stable agreement statistics; uncertainty is still captured in its own field |
| 2026-07-28 | Raw review texts are kept out of the public repo (`data/raw/` and `data/processed/` are gitignored); only labels, review_ids and a small demo sample are published | Re-publication rights for Steam reviews are unconfirmed and full texts could expose user information; reproducibility does not require the data itself, since anyone can rebuild the corpus with the collection scripts and review_ids |
| 2026-08-04 | Pivot from an API-only measurement *audit* to a *training* project: fine-tune a multilingual model, not just prompt an LLM | Build hands-on annotation + fine-tuning experience (the two skills Yifan wants to develop); the lab follow-up deadline is relaxed, so depth beats speed. The original application described "API only, no training" — if this repo is ever sent to the lab, the README must state the training design accurately |
| 2026-08-04 | Human labels become a **gold test set** (~100+/language, hand-labeled, blind, per frozen codebook); a larger **LLM weak-labeled set** (thousands/language) becomes the **training set** | A solo beginner cannot hand-label training-scale data; using LLM labels as training signal is standard **weak supervision / distillation**. The human gold set stays the only trusted evaluation ("exam"), never used for training |
| 2026-08-04 | Add a **cross-lingual transfer** evaluation (train on one language, test on the others) and a three-way comparison (fine-tuned model vs LLM zero-shot vs human gold) | This is the measurement question MilaNLP/AISS care about, and it turns the project from a single-number audit into a real research contribution |
| 2026-08-04 | Training runs on the local ASUS laptop (RTX 4060, 8GB, CUDA); collection/annotation/analysis stay on the Mac; Git is the sync bridge between the two machines | Fine-tuning XLM-RoBERTa-base fits in 8GB; no cloud GPU (Colab) needed. Model `large` or very large data remain a fallback for Colab |
| 2026-08-04 | For appid 730 (Counter-Strike), keep only reviews with `timestamp_created >= 1695772800` (2023-09-27, the CS2 launch) | appid 730 also carries the pre-2023 CS:GO era, which is effectively a different game; date-filtering keeps the "CS2" construct consistent. Battlefield 2042 (1517290) needs no such filter |
| 2026-08-04 | Japanese is the binding language (~14k combined raw reviews); English/Chinese will be down-sampled toward the Japanese count | Keeps the three languages comparable so cross-lingual differences aren't confounded by sample-size imbalance |
| 2026-08-05 | Collect with `purchase_type=all`, not the API default `steam` | A probe (`00_probe_counts.py`) held everything constant and varied only this flag: CS2 Japanese went 6,767 → 11,259, matching the store page's 11,262. The default silently drops key-activated / free-acquired reviews; the effect is largest on free/competitive titles (CS2 zh 3.47x) and negligible on paid single-player ones (Elden Ring ja 1.07x). `all` recovers the full public pool, which matters most for the Japanese bottleneck |
| 2026-08-05 | Add **Elden Ring** (appid 1245620) as a third, different-genre control game | Breaks the shooter genre-monoculture of CS2 + BF2042; supplies hard negatives (praise, and "hard ≠ unfair" cases) so the model learns the *perceived-unfairness* construct rather than "shooter complaint = unfair". Japanese pool under `all` = 5,940, well above the ≥1,000 bar |
| 2026-08-05 | Japanese pool under `purchase_type=all` ≈ 20.4k across the three games (CS2 11,259 + BF2042 3,160 + Elden Ring 5,940), superseding the earlier ~14k estimate | More Japanese headroom than expected. Caveat: CS2's 11,259 still includes pre-2023 CS:GO reviews and will shrink after the date filter (≥ 2023-09-27); the true usable Japanese count is only known after pagination. EN/ZH are still down-sampled toward the post-filter Japanese count |
| 2026-08-05 | CS2 has only **1,943** Japanese reviews from the CS2 era (≥ 2023-09-27); the other ~9,300 of its 11,259 JA reviews are CS:GO-era and are correctly excluded by the date filter | Verified empirically: paginating CS2/ja reached exactly the launch-date floor (oldest kept review = 2023-09-27), so 1,943 is the true usable count, not a pagination artifact. This validates the date-filter decision. Combined usable Japanese ≈ 1,943 (CS2) + 3,000 (BF2042) + 3,000 (Elden Ring) ≈ 7.9k, still well above the ≥3,000/language need |
| 2026-08-05 | **Deep-collection experiment settles the API ceiling: Steam's `recent`+cursor feed is capped far below `total_reviews`, so full back-collection to 2023 is impossible for dense cells.** `01b_deep_collect.py` (resumable, purchase_type=all) paged every EN/ZH cell to `reached_end` (an empty page = the true bottom of the served feed). Hard proof: CS2 Chinese has **1,404,644** total reviews but the feed gave out at **42,797 (~3%)** at 2026-03-05; CS2 English gave out at 118,372 at 2026-02-13. BF2042 English is the only cell that reached game launch (133,203, back to 2021-11-19). | Turns the earlier "residual limitation" from a guess into a measurement. **Consequence for alignment:** compare cross-lingually inside each game's **common window** = the latest of the three languages' oldest dates — bound by **Chinese** in all three: CS2 ≈ 2026-03→now (~5 mo), BF2042 ≈ 2023-11→now (~2.7 yr), Elden ≈ 2025-05→now (~15 mo). Two constraints coexist: Chinese binds the window's *reach* (left edge), Japanese still binds sample *size* within it (sparse). Down-sample EN/ZH by month to the JA count inside the window; document the depth ceiling in the README |
| 2026-08-06 | **Data pool split into five non-overlapping roles**, specced in `data_split_spec.md`: `pilot_draft` (AI reads it to draft the codebook, 30–50/lang), `pilot_prompt` (tune the LLM labeling prompt, 30–50/lang), `gold` (blind eval, ≥100/lang), `dev` (model selection, 50–100/lang), `train` (LLM weak-labeled remainder, thousands/lang). Machine-readable truth = `data/splits/split_manifest.csv` (`review_id,appid,lang,role`; tracked in git since review_ids are publishable). `SPLIT_SEED=20260806`; each stage subtracts already-assigned review_ids ("后减前") so a review has exactly one role | **Two isolation tiers, not one.** Tier 1 (inviolable, breaking = fabrication): `gold` must stay invisible to ALL development (codebook, prompt, few-shot) — else it "passes by construction" and eval is inflated (leakage). Tier 2 (hygiene, breaking ≠ fabrication but self-deception): `pilot_draft` vs `pilot_prompt` best kept disjoint so the LLM isn't just memorizing the same handful baked into the codebook; but overlap here never pollutes the final eval (eval is on gold). Pilots may be carved from the raw pool BEFORE alignment because the unfairness construct is window-independent; gold/dev/train come from the aligned common window. Enforced by review_id manifest, not by memory |
| 2026-08-06 | **Doubled `pilot_draft` (135→270) then collapsed the subtype layer to two: `distributive` + `procedural`** (dropped `interpersonal` + `informational`). | Read all 270 random `pilot_draft` reviews: `procedural` saturates in all three languages, but `interpersonal`/`informational` produced **0 standalone instances** across 270 (only ~1 buried tinge each). Doubling from 135 confirmed this is structural, not a sample-size gap — competitive/single-player review corpora concentrate perceived unfairness on "who gets an advantage" (distributive) and "are the rules/enforcement fair" (procedural). Freeman (2022)'s 25 codes also cluster in these two dims. Resolves codebook §8-Q1. The two dropped dims can still fire the binary main label (PRESENT with empty `subtype`); they are simply not coded as subtypes. Expansion is additive + reproducible: `scripts/02a2_expand_pilot_draft.py`, same SPLIT_SEED, 后减前 preserved. |
| 2026-08-06 | **Purposive (keyword-seeded, hand-curated) top-up of 18 `distributive` examples into `pilot_draft` (270→288), 6 per language.** | Random pilots yielded almost no Chinese `distributive` and no balanced cross-lingual coverage, because the games differ (CS2 = cosmetic skins, Elden = single-player). Since `pilot_draft` is NOT used for measurement, purposive oversampling of likely-PRESENT reviews is legitimate **for codebook drafting only** and MUST be disclosed as non-random. Sources: BF2042 pay-to-unlock (EN/ZH), Elden regional pricing / おま国 (JA), plus explicit "no pay-to-win" hard-negatives. Script `scripts/02a3_purposive_distributive.py` (curated ids hard-coded = the reproducible artifact); provenance in `data/splits/pilot_draft_purposive_distributive.csv`. **Firewall intact:** gold/dev/train are carved later from the aligned pool with their own 后减前, and **gold must stay random / by true distribution — never purposive.** |
| 2026-08-08 | **`out_of_scope` no longer collapses to `ABSENT` — main label becomes three-valued `{PRESENT, ABSENT, NA}`** (codebook rev3, §2/§3/§7). `out_of_scope=true ⇒ unfair_label=NA`. | Collapsing garbage/links/foreign-spam into ABSENT poisons the negative class three ways: (1) the model learns a "short+symbols=ABSENT" shortcut instead of the hard "has-content-but-not-unfair=ABSENT" boundary; (2) per-language noise rates differ (RU/TR spam heaviest in CS2/ja), so noise-rate differences leak into "unfairness-rate differences" and confound the cross-lingual comparison — the one thing the project must avoid; (3) the two kinds of ABSENT become irreversibly indistinguishable. NA rows are kept for audit but **excluded from train/eval**. **Consequence:** gold's "≥100/lang" now means ≥100 **codable** (out_of_scope=false); since ~half of a random draw is noise, gold needs **noise-rate top-up** (draw → mark out_of_scope → randomly top up to ≥100 codable, top-up still 后减前, randomness intact). |
| 2026-08-08 | **`procedural` gains a `procedural_facet` sub-layer (5 provisional facets), plus author-stance gate, subtype tie-breaker, `subtype=other`, irony three-step, and positive-example gate** (codebook rev3). | Addresses an external review: `procedural` was a catch-all (matchmaking/cheating-governance/bans/access-exclusion/balance) that would absorb most positives and kill error analysis. Facets: `cheating_governance`, `sanction`, `access_exclusion`, `balance_rng`, `unfair_by_design`. **Same discipline as the interpersonal/informational collapse**: facets are provisional; at freeze, counted on (pilot试标 + labeled gold), a facet is kept only if its **three-language aggregate ≥ 15** standalone instances, else merged back into plain `procedural`. Also: (a) §1.1-B tightened so final attribution must reach the authority/system level (pure player-vs-player = ABSENT) — fixes the "scope instability" critique and resolves Q4 (toxicity default ABSENT, exception = explicit official-tolerance→procedural); (b) Q2 resolved (platform/hardware exclusion → procedural+access_exclusion); (c) `subtype` domain adds `other` so PRESENT never has an ambiguous empty value; (d) tie-breaker for distributive-vs-procedural (pay-to-unlock defaults distributive); (e) positive-example gate "topic ≠ construct" + anchor三件套 (full text + minimal evidence_span + normalized_claim); (f) irony three-step using the `voted_up`/`votes_funny` fields as cross-checks, unresolved→borderline/low-confidence or NA (single annotator still hand-labels every row); (g) "minimal pair"→"contrastive pair" (real reviews differ on >1 axis; true minimal pairs deferred to hand-built pilot probes). |
| 2026-08-08 | **Codebook rev4** (external-review round 2): (a) **`procedural_facet` keep/merge counted on `pilot_draft` ONLY, never gold** (chosen: option a — enlarge pilot if too thin); (b) **stance gate splits "retracted" vs "resolved-but-still-aggrieved"**; (c) **`access_exclusion` no longer auto-fires** — must carry a normative cue; (d) **facets sharpened on a PvE/PvP axis** (`balance_rng`→`competitive_balance` = PvP edge; `unfair_by_design` = PvE design illegitimacy); (e) **distributive anchors re-audited** — 165149867/202254837 demoted to ABSENT/borderline, 164972778 recast as a Q7 negative; (f) **Q5/Q6/Q7 resolved**; (g) **explicitness/confidence/metadata operationalized** (§6.1). | **Fixes a genuine leakage bug I introduced in rev3**: counting facets on "pilot + labeled gold" would let gold shape the taxonomy, contradicting the freeze-before-gold red line (gold must stay a blind exam). This also **retracts** an earlier claim this session ("gold alone can test ≥15, so pilot needn't grow") — with gold off-limits, the facet test rests entirely on `pilot_draft`, so pilot may need purposive procedural top-up before freeze. Anchor re-audit was empirically verified against raw text: 202254837 *recommends* buying the elite edition (no grievance), 165149867 merely *describes* battle-pass unlocks ("好在配件丰富"), 164972778's only paywall gripe is cosmetic-only — all fail the §4.0 "topic ≠ construct" gate. **Q5** two-tier versioning: v1.0 frozen and never edited in place; a label-flipping change → v2.0 + re-label, editorial → v1.0.x. **Q6** normalized_claim required for PRESENT and for borderline ABSENT only. **Q7** distributive scope = competitive-advantage / gameplay-affecting content·access + differential pricing; purely cosmetic monetization grievance = ABSENT. Codebook stays `v1-draft` (NOT frozen). |
| 2026-08-08 | **Pilot 试标 (all three languages) + procedural top-up (补样) executed against codebook v1-draft rev4.** Claude applied rev4 to every `pilot_draft` review and emitted structured §7-schema labels (`data/pilot/pilot_labels_claude.jsonl`, no raw text → publishable; worksheet-with-text kept in gitignored `data/raw/pilot_worksheet.jsonl`). Scripts: `03a` (build worksheet), `03b` (labels), `03c` (facet counts), `03d` (candidate scan), `02a4` (top-up append). **Facet ≥15 test (counted on `pilot_draft` ONLY, never gold — §9-gap4 firewall):** on the original 288 rows every facet was thin (cheating_governance 5, sanction 2, access_exclusion 3, unfair_by_design 6, competitive_balance 0), so a purposive procedural top-up was curated (45 read-and-verified rows, `02a4` + `pilot_draft_purposive_procedural.csv`, pilot_draft 288→333). **Final verdicts:** `cheating_governance` 15, `sanction` 15, `unfair_by_design` 18 → **KEEP**; `access_exclusion` 10 (EN/ZH only, 3 borderline) → **borderline, Yifan decides (widen net or fold)**; `competitive_balance` 3 → **FOLD into plain `procedural`** — confirms the codebook's "证据最弱" flag: genuine PvP-unearned-edge is sparse, most "balance" hits are design-quality (ABSENT) or collapse into cheating_governance. | **Cross-lingual imbalance is a real finding to disclose (Hershcovich):** surviving facets are NOT evenly spread — `unfair_by_design`(理不尽) is JA/Elden-dominant (ja17/zh1/en0), `sanction`(EA account bans) is ZH-BF-dominant (zh13). Base rates: PRESENT = 73/333 with top-up (28/288 = ~10% before). **9 items flagged for Yifan's adjudication** (annotator-of-record): (1) §3 anchors `194432373`/`200365947` marked out_of_scope but carry an evaluable stance → should be ABSENT (language≠scope; a trailing meme doesn't rescue an otherwise codable review); (2) `103109840`(EN)/`103331879`(JA) — the lone informational/interpersonal cues §4.2.1 notes: §1.1 permits other-PRESENT but both read as rhetorical summaries of a quality/bug complaint → currently ABSENT-borderline; (3) borderline PRESENTs `224146374`/`175291341`/`110932128`/`111346079` on §5-A/§5-C/Q3 boundaries. **NA-vs-ABSENT gap surfaced:** §3 lacks an explicit rule for bare sentiment tokens ("好"/"fun"/"Good"/"神") — I applied "gibberish/off-topic/meme = NA, any interpretable stance = ABSENT" but flagged the inconsistency (immaterial to PRESENT/facet analysis). **Firewall verified:** manifest 468 rows all-unique, 0 `gold` rows (not drawn), labels carry no raw text, every PRESENT has a subtype. These are Claude's DRAFT pilot labels for Yifan's review; codebook stays `v1-draft` (NOT frozen) — rev5 (fold competitive_balance, resolve flags, disclose top-up + cross-lingual skew) proposed, awaiting Yifan. |
| 2026-08-08 | **Yifan's pilot adjudications + two codebook rules (→ rev5 label pass).** Annotator-of-record ruled on the flagged rows and set two general rules; labels regenerated at `v1-draft-rev5`. **Row rulings:** `224146374`→PRESENT/unfair_by_design (strong, borderline dropped: reviewer explicitly contrasts legitimate difficulty "盖尔=享受" vs illegitimate design "数值用脚填/神经刀" → clears §5-A); `175291341`→PRESENT/access_exclusion (kept borderline); `110932128`→ABSENT (names 理不尽 but terse positive review — topic≠construct, §4.0); `111346079`→ABSENT (§5-C framerate complaint dominant, lone price line too thin for distributive); §3 anchors `194432373`/`200365947`→ABSENT (language≠scope). **Rule #2 — drop `other` subtype entirely** (no informational/interpersonal): the two `other` rows (`140004357`/`202517276`) were misleading-payment grievances already carrying `distributive`, so folding loses no PRESENT; subtype domain is now exactly `{distributive, procedural}`. **Rule #3 — bare-sentiment × metadata closes the §3 NA-vs-ABSENT gap:** an interpretable bare token whose sentiment *agrees* with `voted_up` → ABSENT (consistent, codable); a token that *contradicts* `voted_up` → NA; uninterpretable gibberish/meme stays NA. Applied over 69 short NA rows: **37 flipped NA→ABSENT** (好/GOOD/神/最高/爽/…, all up=1, plus 史=up=0), the clean contradiction `113628031`(👍 but up=0) stays NA, 6 ambiguous/meme tokens (图一乐/shi/情书在哪/俺も苦しんだ/全部・2年・anomaly) stay NA per Yifan. **Post-adjudication tallies:** PRESENT 71/333, NA 83→46; facets `cheating_governance`15·`sanction`15·`unfair_by_design`17 KEEP, `access_exclusion`10 THIN, `competitive_balance`3 THIN (fold). | **Rule #3 makes metadata part of the annotation decision — this forces a freeze-blocking design choice (raised by external review, §-metadata):** if the human annotator uses `voted_up`/`votes_funny` (for irony §5-F *and now* the bare-word rule) but the model sees text only, gold encodes signal the model cannot access → unfair eval. Resolution to lock before freeze: **give the model the same metadata fields the annotator uses** (`voted_up`, `votes_funny`, `votes_up`, `received_for_free`) — the pilot worksheet already carries them. The two dropped `other` rows confirm informational/interpersonal never produced a standalone PRESENT across 333 rows, consistent with the 2026-08-06 subtype collapse. All edits at source (`03b`), regenerated + re-counted (`03c`); firewall unchanged (0 gold, no raw text in labels). |
| 2026-08-05 | **Temporal-window confound identified and (partly) addressed.** With `filter=recent` + a fixed per-language target, a fixed count covers very different time spans per language (CS2/en 3,000 ≈ 2 days vs CS2/ja ≈ 2 years). Fix: collect each game's Japanese in full to define its time floor, then collect EN/ZH back to that floor (early-stop once older; safety cap 15k) so the raw pools share a window; later down-sample by month (stratified) toward the JA count | Cross-lingual differences must not be confounded with time. Chosen over "document only" and over "restrict all to a short common window" (which would starve Japanese). **Residual limitation:** Steam's API is newest-first with no date-range query, so the densest cell (CS2/en) still cannot reach the JA 2023 floor within the cap (~10-day window); this is documented in the README limitations, not hidden |
| 2026-08-10 | **Cross-lingual top-up (→ rev6) + two-axis facet framework.** Yifan's challenge: the facet↔language skew might be *my sampling*, not the world — "定向去补充每个标签下缺少的语种". Executed a THIRD purposive top-up (`02a5_purposive_crosslingual.py`, +20 hand-verified PRESENT into pilot_draft 333→353), consciously searching the SAME game a facet lives in, in the *empty* languages, each hit read in full against the §4.0 gate. Split the old single ≥15 test into **two orthogonal axes** in `03c` (now with a game-locus join): **Axis 1 KEEP/FOLD** = aggregate ≥15; **Axis 2 cross-lingual** = ≥3 *per language* (floor set by Yifan; aggregate ≥15 does NOT prove cross-lingual). **Empirical findings:** (a) `unfair_by_design` EN/ZH gap was a **sampling hole, not a real gap** — Elden has abundant EN "artificial difficulty / input reading" + ZH "读指令/作弊AI/理不尽"; filled to **4/4/16=24**; (b) `competitive_balance` JA "sparse" was a small-pilot artifact — the full BF dump is full of structural one-sided-balance complaints (一方的/陣営有利/兵器過強+対抗手段なし); reached **3/3/3=9**; (c) `sanction` EN/JA genuine unjust-ban instances exist → **4/13/4=21**; (d) `access_exclusion` JA is a **REAL sparse cell** — after multiple seed sets (secure boot/TPM/kernel/課金アンロック/Linux) only ONE genuine JA instance (`201569269`), the rest cosmetic battle-pass or §5-C tech; **NOT force-inflated** (ja stays 1). | **The two axes now cleanly separate KEEP from cross-lingual, and disentangle language from game (the deepest confound: Elden≈JA, BF≈ZH/EN).** Verdicts: **3 facets pass BOTH** — `cheating_governance` 15 (3/8/4, **cross-lingual & genuinely cross-game** BF11+CS2·4, the strongest), `sanction` 21 (4/13/4, cross-lingual but **BF-dominant** BF20/CS2·1), `unfair_by_design` 24 (4/4/16, cross-lingual but **Elden-only → title-specific**). **`competitive_balance` 9** = cross-lingual (3/3/3) **but** below KEEP-15 (BF-only) → *cross-lingual-yet-thin*; **`access_exclusion` 11** = **bilingual EN·ZH** (ja=1 sub-floor, real finding) + below KEEP-15 (BF-only). **Answer to "when may we claim cross-lingual":** need ≥3 PRESENT in each language AND, to claim language-generality rather than title-specificity, ≥2 games — only `cheating_governance` clears both. **Two decisions still open for Yifan (freeze-blocking):** (A) `competitive_balance` — keep as a cross-lingually-attested-but-rare facet, or fold into plain `procedural`? (B) `access_exclusion` — keep as an EN·ZH bilingual facet, or fold? plus how Axis-1(≥15) and Axis-2(≥3/lang) interact when they disagree. Firewall unchanged: pilot_draft only, provenance in `pilot_draft_purposive_crosslingual.csv`, 0 gold rows, no raw text in labels; version stamped `v1-draft-rev6`. |
| 2026-08-10 | **Yifan resolves the two freeze-blocking facet decisions; codebook written to rev6.** (A) `competitive_balance` → **KEEP** as a facet (not fold), despite Axis-1 aggregate 9<15 and BF-only: it is a clean cross-lingual construct (Axis-2 3/3/3) and a distinct PvP dimension; the sparsity + single-game locus are disclosed, not hidden. (B) `access_exclusion` → **KEEP as a full (potentially tri-lingual) facet**, NOT demoted to bilingual: the current pilot's ja=1 is a real sparse cell, but "no more JA in this pilot" ≠ "the construct never occurs in JA" — mark it as *JA under-attested in current pilot*, and demote to a bilingual facet **only if a genuinely exhaustive search still yields zero JA**. Axis rule: **the two axes are reported separately and never override each other** (KEEP/FOLD from Axis-1, cross-lingual claim from Axis-2). | **All §8 open questions (Q1–Q8) are now resolved; codebook rev6 written (`codebook/codebook_claude_v1.md`).** rev6 also folds in the previously-unwritten **rev5** (drop `other` subtype → domain `{distributive, procedural}`; misleading-paywall `140004357` into distributive) so the codebook body finally matches `03b`'s labels. Substantive rev6 edits: (1) facet keep/cross-lingual recast as **two orthogonal axes** with per-facet **locus** + rev6 counts/verdicts (§4.2.1); (2) all three purposive top-ups disclosed (pilot_draft→353; §9-gap3); (3) **§8-Q8 cross-lingual-claim standard** made explicit — 5 conditions + operational floor (≥3/lang AND ≥2 games for language-generality); (4) **metadata parity** codified (§4.0): the same non-identity metadata enters BOTH human annotation AND model inference; (5) **explicitness null-rule** (PRESENT-only) + **confidence redefinition** ("certainty about the final label", §6.1); (6) **Freeman empirical wording toned down** (§9-gap1): "direction-consistent", not "coded per Freeman" or "facets validated by Freeman". Still `v1-draft-rev6` (NOT frozen); firewall unchanged (0 gold, no raw text in labels). Next: close §9 gaps (Freeman per-code table / single-annotator cross-lingual drift test) before any freeze. |
| 2026-08-10 | **Codebook rev7 (external-review round 3 + Yifan's 3 scoping calls).** Facet keep/fold reframed: the old "Axis-1 KEEP / Axis-2 FOLD" naming (whose "objective thresholds" were then overridden by human KEEP exceptions, and whose counting script still printed contradictory `FOLD/thin`) is replaced by **four descriptive dimensions** (`support_volume` / `language_coverage` / `title_coverage` / locus) **+ one human disposition `taxonomy_status ∈ {core, exploratory, folded}`** — thresholds describe, status decides (codebook §4.2.1). Axis-1 threshold renamed **"dev-sample coverage floor" (NOT frequency)**; aggregate counts explicitly ≠ natural frequency (only random gold reports frequency). Cross-lingual/sparsity wording made **conservative**: access_exclusion JA "real sparse" → "under-attested; cannot yet distinguish real sparsity / mechanism difference / recall shortfall"; secure-boot "Western+Chinese phenomenon" claim deleted. **Interpersonal/informational cut cleanly from §1.1** (Yifan: out of scope this study, NO safety valve — pure interpersonal/informational grievance = ABSENT). **Freeman scoped to sensitizing-concepts-only** (Yifan: coding table unobtainable → declare background/sensitizing use, all label boundaries defined independently + dev-set validated) and **removed from the freeze-gap list** (§9.1). `uncertainty_reason` **controlled vocabulary** defined (§6.0, 7 values); the 19 existing borderline rows' generic "borderline per §6" deferred to the human validation pass (no mechanical re-tag, per Yifan's "no extra labeling churn"). `anchor_catalog` split into its own artifact (script `03e`; full-text `data/raw/anchor_catalog.jsonl` gitignored + publishable `data/pilot/anchor_catalog.jsonl`). §9 split into **9.1 literature-source limits / 9.2 project freeze-gates / 9.3 freeze-gap-closure table**. Cross-file sync: `data_split_spec.md` pilot size noted as 353, `03c` drops `other` + prints `taxonomy_status` instead of `FOLD/thin`. **Supersede note (closes a cross-file inconsistency):** the 2026-08-06 decision "the two dropped dims can still fire PRESENT with empty `subtype`" is **superseded by rev5** — subtype domain is exactly `{distributive, procedural}` and every PRESENT must carry a subtype; logged here so the old row is not read as current. | **Fixes the self-contradiction the reviewer caught:** two "objective" facet thresholds were being overridden post-hoc by human KEEP exceptions, and the script's `FOLD/thin` output disagreed with the codebook's KEEP rulings — both dissolved once thresholds are purely descriptive and `taxonomy_status` carries the disposition (competitive_balance/access_exclusion = `exploratory`, not "threshold-violating exceptions"). **Explicitly NOT done (Yifan):** no `distributive` re-tag of the P2W competitive_balance example (`225680958`) — avoids extra labeling churn; the subtype↔facet co-tagging question is left for the human validation pass, not forced now. Firewall unchanged (0 gold, pilot_draft only, no raw text in labels); still `v1-draft-rev7` (NOT frozen). |
| 2026-08-10 | **Freeze-prep package built (codebook stays `v1-draft-rev7`, NOT yet frozen).** Created `freeze/preregistration.md` (locks §9.3 #1 blind-check + #2 test-retest thresholds BEFORE running: main-label Cohen's κ≥0.60, PRESENT-vs-rest specific agreement≥0.70, cross-language κ spread≤0.15; anchor density = 1 positive+1 hard-neg+1 borderline per facet). Created `freeze/input_schema.md` (§9.3 metadata gate: the 7 non-identity metadata fields + appid/lang fed identically to human annotator AND model; a text-only baseline arm defined; `curated_tag`/`curated_reason`/`provenance`/identity explicitly excluded — feeding the purposive seed tags would leak the intended label). Extended `scripts/03e` so `anchor_catalog` carries positive+hard_negative+borderline per facet (27 anchors, hard-neg/borderline drawn ONLY from Yifan-adjudicated rows, ran with 0 role-inconsistencies; 1 gap = unfair_by_design/borderline, flagged); emits `freeze/anchor_review_sheet.md` (gitignored — carries raw excerpts) for Yifan's per-anchor ratification. Added `scripts/04a_retest.py` (single-annotator cross-lingual test-retest: build carves 54 ids [18/lang, 7P/8A/3NA], blank sheet gitignored; score computes per-language κ + confusion + PRESENT-specific agreement) and `scripts/04b_codebook_check.py` (independent blind check: excludes the 73 used-in-drafting ids = codebook body ∪ anchor_catalog, carves 59 from 280 eligible; a fresh Claude labels blind, score vs reference face). `.gitignore` extended for `freeze/anchor_review_sheet.md`. Codebook §9.3 now points to all freeze artifacts. | **Pre-registration is the honesty lock: pass/fail thresholds are fixed before any κ is seen, so no post-hoc grading.** All check/retest sets draw from pilot only (0 gold rows — firewall intact); raw-text sheets are gitignored, id manifests tracked. **Hard timing constraint surfaced:** §9.3 #2 test-retest requires T1→(10–14 day gap)→T2, so v1.0 CANNOT be frozen the same day; the earliest freeze is ~10–14 days after T1 unless Yifan waives/relaxes that gate. Awaiting Yifan's approval of the pre-registered thresholds + anchor sheet before running the checks and freezing. |
| 2026-08-10 | **Yifan's three freeze-prep calls.** (1) **test-retest (#2) WAIVED for v1.0** — the 10–14 day T1→T2 gap would push freeze back ~2 weeks; the pilot relies on the independent blind check (#1) alone. Cost disclosed as a limitation: v1.0 does NOT quantify single-annotator cross-lingual judgment drift (a known threat for one-person tri-lingual gold, confoundable with "model worse in language X") → goes in README/paper limitations; `scripts/04a_retest.py` harness kept for v2. (2) **Blind-check main-label floor set to κ≥0.65** (stricter than the 0.60 acceptable-floor); PRESENT-vs-rest specific agreement stays ≥0.70; `04b` KAPPA_FLOOR updated. (3) **Independent blind labeller = a FRESH Claude instance** (codebook + check sheet only, no drafting memory) as the "independent stranger" proxy, Yifan adjudicates disagreements. `freeze/preregistration.md` updated accordingly (§1 floor, §2 marked waived, §4 gate list). | **Freeze can now complete without the 2-week wait, gated on: #1 blind check passing κ≥0.65 + Yifan ratifying the anchor sheet (incl. the unfair_by_design/borderline gap) + the done-gates holding.** Waiving #2 is a deliberate rigor-for-speed trade recorded here so it is disclosed, not hidden. Blind labelling to be run now via a fresh Claude pass; then `04b score`. |
| 2026-08-10 | **Blind check PASSED + codebook patched to close the one real gap it found + anchor sheet ratified (codebook STILL `v1-draft-rev7`, freeze pending Yifan's final OK).** Independent blind check (`04b`, fresh Claude, 59 rows): main-label **κ=0.77 (PASS vs 0.65)**, PRESENT-vs-rest **κ=0.89 (PASS vs 0.70)**, raw 0.88. Disagreements: 2 PRESENT→ABSENT (genuine attribution edges, no rule change) + **5 ABSENT→NA all clustered on ONE codebook defect** — the rev5 "bare-sentiment × metadata" rule (interpretable bare token whose sentiment agrees with `voted_up` → ABSENT; contradicts → NA; gibberish/meme → NA) was applied to the labels (37 NA→ABSENT flips) + lived in decision_log but was NEVER written into §3, which literally listed 神/夯/一句好评 → NA. **Fixed:** §3① de-staled (removed 神/夯 from auto-NA), new boxed "裸情感 × metadata" rule written into §3 body, §0 "一句好评=噪声" reconciled (doc now matches labels; no re-labelling). **Anchor ratification (Yifan):** 4 borderline-ABSENT anchors reviewed — `153192679`「G哥太多了」KEPT ABSENT hard_neg (pure prevalence, no (B) attribution; promoting it would delete cheating_governance's (B) requirement + contradict `229414199`); `228759578`(needs anti-cheat)→cheating_governance **PRESENT positive**; `226675676`(反讽无故封号)→sanction **PRESENT positive**; `228424527`(「我不是程序员不该这么难」secure boot)→access_exclusion **borderline-PRESENT** (Yifan reads it as excluding non-technical players; flagged: this inverts §5-C's written ABSENT poster-child + sits on the access_exclusion definition line, `borderline=true` kept to mark it, §5-C lines 189/211/212 rewritten to teach 228424527↔162971869 contrast). 3 pilot reference labels flipped ABSENT→PRESENT (dist 91/216/46 → **94/213/46**). **Backfills (all from already-adjudicated rows, zero new sampling):** access_exclusion/hard_neg=`162971869`(states secure-boot req, no norm cue→ABSENT, razor-sharp pair with promoted 228424527); unfair_by_design/borderline=`165149867`(battlepass-unlock grind = monetization/distributive edge→ABSENT-borderline, fills the long-standing gap); cheating_governance/borderline=`103037208`("Kernel-level anticheat malware", intrusion/privacy edge). **sanction/hard_neg WAIVED** — no clean ABSENT ban-topical row exists in pilot (sanction-topical rows nearly all ruled PRESENT; informative absence). `03e` rebuilt → **30 anchors, 0 role-inconsistencies**; every facet has positive + ≥1 boundary role. §4.2.1 positive list synced. | **The blind check earned its keep: κ passed the pre-registered floor AND surfaced a genuine doc-vs-labels contradiction that would otherwise have been frozen in permanently.** Per the pre-registration's "fix the codebook rule, not the threshold" spirit + the never-edit-in-place property of a freeze, the §3 gap was patched even though κ passed. Firewall intact: the 3 flipped rows were anchors → excluded from the blind-check set (cited_ids unions anchor_catalog), so **κ=0.77 is unaffected** (verified: none in codebook_check_manifest); 0 gold rows; raw text local only. **Freeze NOT yet executed** — awaiting Yifan's final OK to bump `v1-draft-rev7` → `v1.0`. Only after freeze: random gold sampling. |
| 2026-08-10 | **🔒 CODEBOOK FROZEN → `v1.0` (Yifan explicit approval).** Final pre-freeze adjustment: `228424527` reclassified from a POSITIVE anchor to a **BORDERLINE** anchor of `access_exclusion` (Yifan: genuinely on the edge, not a clean positive) — label stays PRESENT/`borderline=true` (PRESENT-borderline), distribution unchanged at **94 PRESENT / 213 ABSENT / 46 NA**. This gives `access_exclusion` the ideal razor triple: positive (`114083525` etc.) / hard-negative `162971869` (bare secure-boot statement, no norm cue → ABSENT) / borderline `228424527` ("我不是程序员不该这么难" exclusion语气 → PRESENT-borderline). `03e` ANCHORS synced (moved 228424527 positive→borderline; header comment corrected to "2 promoted to positive, 228424527 kept borderline"); codebook §4.2.1 line 185 removed 228424527 from the positive list and points it (+`162971869`) to the anchor_catalog as border/hard-neg; §5-C (189/212) already said "borderline-PRESENT" so needed no change. `03e` re-ran → **30 anchors, 0 role-inconsistencies, 1 waived gap (sanction/hard_neg)**. **Freeze gates (freeze/preregistration.md §4) all met:** (1) blind check κ=0.77 main / 0.89 PRESENT-vs-rest, both PASS ≥0.65/≥0.70 and verified UNAFFECTED (3 flipped ids all anchor_catalog rows → excluded from check manifest); (2) test-retest #2 WAIVED (disclosed limitation); (3) anchor sheet ratified incl. all gap dispositions; (4) §9.3 residual rows closed. Codebook header `codebook_version` bumped `v1-draft-rev7` → **`v1.0`** with 🔒 freeze stamp (2026-08-10, Yifan approved). | **v1.0 is frozen: from here it is NEVER edited in place — any change that could flip a label = `v2.0` + re-label gold/LLM; pure-editorial = `v1.0.x`, no re-label (§8-Q5).** Firewall intact at freeze: **0 gold rows** touched by any development; all anchors/checks drew from pilot only; raw text local/gitignored, only labels + id manifests publishable. **Order rule honored: freeze FIRST, gold AFTER.** Next authorized step (awaiting Yifan's separate go): random gold sampling by true distribution (后减前, invisible to all development the instant it is drawn) — NOT yet human gold annotation, NOT yet LLM weak-labeling; each of those is a later, separately-gated step. |
| 2026-08-11 | **🚪 GOLD IDs DRAWN (one-way door crossed, Yifan explicit go "开始进行首次id抽样").** Ran `scripts/02_align_sample.py --draw-gold`. First materialized `machine_eligible_frame` (window `timestamp_created ≤ 2026-08-01 UTC`, no lower bound; dedup on recommendationid; light content filter = drop empty/pure-symbol, NO min-length; Steam-language buckets; 后减前 the 488 pilots) = **419,827 rows** (EN 317,226 / ZH 91,952 / JA 10,649; frame → gitignored `data/processed/machine_eligible_frame.csv`, review_id only). Then drew gold: **three-language equal-200 (600 total)**, within each language allocated across the 3 games by the game's share of that language's frame via **largest-remainder (Hamilton)**, each game×lang cell independently seed-shuffled (`f"{SPLIT_SEED}-{appid}-{lang}"`), gold = first n, remainder = NA-backfill reserve order. Realized 9-cell: EN Elden48/BF83/CS2·69, ZH Elden43/BF68/CS2·89, JA Elden109/BF57/CS2·34. Wrote 600 gold rows → `split_manifest.csv` (now 1089 = 600 gold + 353 pilot_draft + 135 pilot_prompt), per-cell design weights (EN≈2.26/ZH≈0.66/JA≈0.076) → `data/splits/gold_design_weights.csv`, reserve order (419,227 rows) → `data/splits/gold_reserve_order.csv`. **Integrity verified:** 0 duplicate review_ids, gold ∩ pilots = 0, manifest carries NO text column (review_id/appid/lang/role only). | **Order rule honored: freeze codebook (2026-08-10) → freeze sampling scheme (2026-08-11) → draw gold IDs (2026-08-11), in that order.** Ordering firewall intact: drawing gold **IDs** (review_id only, no text) is allowed before the annotation-prompt freeze; **opening/labeling gold text is NOT** until the prompt is frozen. Design weights restore the observed eligible-corpus composition ONLY (机械合格帧), NOT Steam 全平台/全历史/玩家人口 (capped by the API `recent`-depth ceiling). This is irreversible: the 600 review_ids and their seed-fixed order are now locked. NOT yet done (each separately gated): open/label gold text, prompt tuning/freeze, dev/train carve, LLM weak-labeling, stress-set extraction. |
