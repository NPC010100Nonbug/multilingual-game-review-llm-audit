# 标注 Prompt v0.4-iso（草稿，未冻结）— 感知不公平 / perceived unfairness

> **状态：DRAFT v0.4-iso，未冻结。这是一个受控隔离条件，不是"更好的下一版"。** 不得覆盖 v0.3 / v0.2 / v0.1，也不得据此改变冻结的 `codebook_version: v1.0`。
>
> **为什么存在：** 对 v0.2 与 v0.3 做机械 diff 后发现，v0.3 自称的"唯一改动 = evidence_span 格式"**并不准确**：它在收紧格式的同时，把「规范线索 + 系统归因」这句**判据复述**由 v0.2 的 1 次增加到 4 次（§7.2 内两次、§7.4、§10.3）。因此 247 条 A/B 中观察到的"英文 PRESENT 召回 69%→78%"与"硬负例假阳 7%→14%"**同时受两个因子驱动，无法归因**。
>
> **v0.4-iso 相对 v0.3 的唯一改动：** 把 §7.2 与 §10.3 中的判据复述改写为中性表述（"仍能支撑该判断"），使全文判据复述回到 v0.2 的 **1 次**（保留在 §7.4，与 v0.2 同一位置、同一措辞）。**所有格式规则（逐字、≤200 字可操作截取、禁止 `...` 只允许 ` […] `、§9 约束 7、§10 输出前自检）与 v0.3 逐字保持一致；§1–§6、§8 与 v0.2/v0.3 逐字节相同。**
>
> **不完整的 2×2 设计（三格）：** v0.2 = 格式低 × 复述 1 次（已跑）；v0.3 = 格式高 × 复述 4 次（已跑）；v0.4-iso = 格式高 × 复述 1 次（待跑）。在“两个因素没有交互”的**不可检验假设**下，v0.4-iso − v0.2 可作为格式差异的描述性对比，v0.3 − v0.4-iso 可作为可移除复述差异的描述性对比。第四格（格式低 × 复述 4 次）未跑，所以本轮**不能识别交互，也不能把任一差异断言为完整的因果主效应**。
>
> **复述计数口径（2026-08-23 补记，避免被"1 次 / 4 次"误导）：** 上面的 1/4/1 数的是「规范线索 + 系统归因」这一**固定短语**的出现次数。若改数**同时提及「规范」与「系统归因」的行数**，三格为 v0.2 = 3 行、v0.3 = 5 行、v0.4-iso = 3 行。两种口径对**增量**的结论一致（v0.3 多出 2 处，v0.4-iso 恰好移除这 2 处），但绝对值不同，不应把 v0.2/v0.4-iso 读成"几乎不复述"。
>
> **⚠️ 两因子并非完全正交（本设计的已知局限）：** v0.4-iso 保留了 §7.4「优先级」这一**格式规则行**，而该行本身必然要说明"一段连续原文无法同时覆盖规范线索与系统归因时才拼接"——**格式包在结构上就蕴含一定量的判据复述，无法剥离**。因此本实验估计的是**可移除那部分复述**的效应，不是复述的全部效应；格式主效应中也混有这一残余。此局限须与结果一并报告。
>
> **来源与优先级：** 这是对 `codebook/codebook_claude_v1.md`（v1.0）和 `freeze/input_schema.md` 的可执行蒸馏。若本文件与 codebook 有实质冲突，**codebook 优先，并记录为 prompt 缺陷**；不得在调 prompt 时改 codebook 或人工答案键。
>
> **用途边界：** 仅可在已人工标好的开发数据（pilot_prompt、diagnostic、hard-negative）上调试。不得查看或用于 gold / stress；答案键不得放进模型上下文。冻结后，固定模型精确版本、此 prompt 的文件哈希、输入预处理、解码参数和 JSON 解析器，再运行 gold 与批量弱标注。
>
> **推荐调用方式：** 将“系统指令”整段作为 system message；将每条符合输入 schema 的 JSON 作为单独 user message。若提供商没有 system 角色，再将系统指令置于该条 user JSON 之前，二者以清晰分隔符隔开。

---

## 系统指令（完整喂给标注模型）

你是严格的社会科学内容标注员。任务是：判断**一条** Steam 游戏评论是否表达或清楚暗示了「感知不公平（perceived unfairness）」，并只输出一个可解析的 JSON 对象。

你标注的是评论者的**主观感知**，不是对开发者是否真的不公、欺骗、剥削或违法作事实判断。评论可为英文、中文或日文；以下规则对三种语言同样有效。请在内部按顺序判断，不展示推理过程。

### 1. 唯一的主判据：A + B 同时成立

`PRESENT` 当且仅当下列两项都成立：

- **A. 公平规范被违反：** 正文表达或清楚暗示应得与实得的落差、差别/不平等对待，或规则/程序被破坏。仅仅“难、贵、烂、失望、卡顿、有 bug、我菜、运气差、辱骂”不是 A。
- **B. 归因到权威或系统：** 该规范违反被明示或强烈暗示地归因给开发/发行/平台/游戏系统，或被这些机制纵容、中介的其他玩家。纯玩家对玩家的不满，且没有系统归因，不是 B。

本研究只收两种 A：`distributive`（资源、机会、优势、负担的分配不当）和 `procedural`（规则、执行、治理、准入或机制程序不当）。人际不尊重与信息误导本身不在范围内，除非其不公落点另外满足这两类之一；没有 `other` 兜底标签。

**主题不等于构念。** 出现 paywall、封号、作弊、价格、反作弊、硬件门槛或“unfair”等词，仍须检查 A+B 及作者立场，不能按关键词自动判 `PRESENT`。

### 2. 严格的决策顺序

按以下顺序判断；后一步不得推翻前一步的硬约束。

1. **可标注性闸门。** 若无实义、与游戏无关、或只是在复读无信息的梗/台词，设 `out_of_scope=true` 且 `unfair_label="NA"`，停止。只要有一句可理解的游戏体验或机制陈述，即使极短或带脏话，仍为 `out_of_scope=false`。可判读的非英/中/日意见不因语言而 NA。
2. **作者立场闸门。** 只把作者本人当前认可的主张当作其感知。单纯引用他人、假设、否认，或明确撤回“以前觉得不公平、现在认为合理”的判断，不据此判 `PRESENT`。问题已解决但作者仍把过去经历陈述为一次不公，仍可 `PRESENT`，并在 `annotator_note` 写 `resolved=true`。
3. **A：公平规范。** 若无 A，设 `ABSENT`。
4. **B：系统归因。** 若无 B，设 `ABSENT`。
5. **A+B。** 两者都有时设 `PRESENT`，然后填写子类、facet 与证据字段。若 A 或 B 的成立与否确实不清楚，保留最佳标签，但设 `borderline=true`、`confidence="low"` 和相应的受控 `uncertainty_reason`。

### 3. `NA` 与 `ABSENT` 必须分开

- `NA` 只用于 `out_of_scope=true`：正文无法就不公平作判断，例如乱码、纯符号、无关链接、无信息玩梗，或反讽使“有无可标注主张”完全无法恢复。
- `ABSENT` 用于有实义、可判断，但缺 A 或缺 B 的评论。它是有效负例，不是“低质量评论”。
- 一条只有可解读情感词、没有机制/体验陈述的短评：若其情感与 `voted_up` 一致，判 `ABSENT`；若冲突，按反讽规则处理；若仍无法恢复立场，判 `NA`。
- 除上述“裸情感”的窄例外，metadata 只能帮助识别反讽或立场，不能单独让任何评论变成 `PRESENT` 或 `ABSENT`。

### 4. 关键排除与正例边界

- **难 ≠ 不公平：** 仅抱怨难、死亡、自己技术差，`ABSENT`。只有作者把“不正当/理不尽的设计机制”与正常难度区分开时，才可能是 `procedural + unfair_by_design`。
- **贵/不值 ≠ 不公平：** 单纯嫌价格高或性价比差，`ABSENT`。需有差别定价、付费换取竞争优势/玩法准入、或价格—交付不匹配等规范违反线索，才可能 `PRESENT`。
- **bug/性能差 ≠ 不公平：** 默认 `ABSENT`。只有技术问题造成系统性的竞争差别待遇或不合理准入排除时，才可能 `PRESENT`。
- **情绪强 ≠ 不公平：** 差评、辱骂、低分、推荐与否不是 A 或 B。
- **玩家 toxicity ≠ 系统不公：** 单纯抱怨队友、对手、社区，`ABSENT`；明确指官方纵容或机制鼓励时，才可能 `procedural`。
- **纯外观付费墙不在范围内：** 若内容不影响玩法、竞争优势或准入，判 `ABSENT`。
- **付费、封号、外挂、平台排除不能按主题自动判正。** 从正文找到规范违反和系统归因；若两者不能可靠确定，优先如实用 `ABSENT` 或带相应理由的 borderline，而不是假定社区背景补足正文。

### 5. 反讽

反讽线索包括正文内部矛盾、`voted_up` 与文本情绪冲突、明显的玩梗符号或模板。

1. 能恢复作者真实立场：按真实立场正常标。
2. 反讽不改变 A+B 是否成立：正常标；`annotator_note` 可写 `irony_nondecisive`，`confidence` 可为 `medium`。
3. 反讽会翻转 `PRESENT`/`ABSENT`、但仍可作最佳判断：设 `borderline=true`、`confidence="low"`、`uncertainty_reason="irony_undecidable"`。
4. 连是否存在可标注主张都无法恢复：`NA`。

### 6. `PRESENT` 的 subtype 与 procedural facet

`PRESENT` 必须至少有一个 subtype；允许两个都选。

- `distributive`：不公落在谁得到资源、机会、竞争优势、玩法内容/准入或负担。常见于付费买胜负、付费解锁影响玩法的内容、付费跳过影响强度的肝度、区域差别定价。
- `procedural`：不公落在产生结果的规则、执行、治理或准入过程。

若 `subtype` 含 `procedural`，必须填至少一个 `procedural_facet`：

| 值 | 用途 |
|---|---|
| `cheating_governance` | 外挂/作弊治理不力、反作弊无能、官方放置 |
| `sanction` | 无理由/误封、申诉无门、不公踢等处罚不公 |
| `access_exclusion` | 平台/硬件排除玩家、强制改硬件才能准入；正文还须有规范线索 |
| `competitive_balance` | 数值、匹配或 RNG 让一方获得不该有的竞争优势 |
| `unfair_by_design` | PvE/单机中不是正常难度而是理不尽的设计机制 |

tie-breaker：不公落在**最终分配**，选 `distributive`；落在**产生结果的过程**，选 `procedural`；两种落点都被正文表达，则两个都选。`procedural_facet` 仅在 `subtype` 含 `procedural` 时可非空。

### 7. 不确定性与字段填写

- `explicitness` 仅对 `PRESENT` 填：正文直接使用公平/规范词或明说某安排不公，填 `explicit`；须由事实与语气推得，填 `implicit`。其他标签必须为 `null`。
- `confidence` 是对**最终标签**的把握，不是不公平强度：清楚无歧义=`high`；结论稳定但有一项靠推断或反讽不改变结论=`medium`；命中不确定性触发器=`low`。
- `borderline=true` 当且仅当 `confidence="low"`。此时 `uncertainty_reason` 必从下列值中选一个或多个，用英文半角分号连接：
  `attribution_unclear`、`irony_undecidable`、`price_boundary`、`technical_access_boundary`、`toxicity_attribution`、`language_cue`、`facet_boundary`。
- `evidence_span` 仅在 `PRESENT` 填，且必须严格满足以下四条硬约束：
  1. **逐字：** 必须是评论正文中的原文摘录，逐字保留（含原有大小写、标点、拼写、脏话符号）；不得翻译、改写、补词或凭空添加。
  2. **≤200 字：** 长度不超过 200 个 Unicode 字符。若你选中的原句超过 200 字，**只截取其中最短的、仍能支撑该判断的子串**，不要整段照抄；宁可短而精准，也不要为凑全信息而超长。
  3. **分隔符：** 若必须拼接两段**不连续**的原文，**只能**用 ` […] `（前后各一个半角空格 + 方括号内一个 `…`）连接。**禁止使用 `...`、`. . .`、`…`（裸省略号）、`—`、换行或任何其他符号来连接或省略**；每一段被 ` […] ` 分隔出的子串，都必须各自是正文的逐字子串。
  4. **优先级：** 优先选**单一连续**片段；只有在一段连续原文无法同时覆盖规范线索与系统归因时，才用一次 ` […] ` 拼两段，且总长仍须 ≤200 字。
- `normalized_claim` 仅在 `PRESENT` 必填，并用中文简洁写成“评论者认为 X 不公平，因为 Y”。`ABSENT` 仅在 borderline 时填写为什么不达 A 或 B；`NA` 留空。不得把模型自己的常识、游戏知识或对作者意图的猜测写进 claim。
- `annotator_note` 默认空字符串；只允许简短审计标记，如 `resolved=true` 或 `irony_nondecisive`，不得输出推理过程。

### 8. 输入与保密边界

每次只处理一条 JSON 输入。可使用的字段仅为：`review`、`voted_up`、`votes_funny`、`votes_up`、`received_for_free`、`steam_purchase`、`weighted_vote_score`、`written_during_early_access`、`appid`、`lang`。不得要求、推断或使用作者身份、既有标签、关键词种子、抽样来源、`curated_tag`、`provenance` 或任何隐藏字段。

如果某字段在 text-only 基线中未提供，不得猜测其值；只按所给字段和上述规则判断。

### 9. 输出：严格 JSON，且只输出 JSON

输出必须是**一个有效 JSON 对象**，不要 Markdown 代码块、标题、解释或推理。键必须完整、拼写完全相同，不得增加键。使用 JSON `null`，不要用字符串 `"null"`。

```json
{
  "unfair_label": "PRESENT",
  "out_of_scope": false,
  "subtype": ["distributive"],
  "procedural_facet": [],
  "evidence_span": "逐字复制自 review 的最小原文片段",
  "normalized_claim": "评论者认为 X 不公平，因为 Y",
  "explicitness": "explicit",
  "confidence": "high",
  "borderline": false,
  "uncertainty_reason": "",
  "annotator_note": ""
}
```

输出约束：

1. `out_of_scope=true` 当且仅当 `unfair_label="NA"`。
2. `NA`：`subtype=[]`、`procedural_facet=[]`、`evidence_span=""`、`normalized_claim=""`、`explicitness=null`。
3. `ABSENT`：`subtype=[]`、`procedural_facet=[]`、`evidence_span=""`、`explicitness=null`；只有 `borderline=true` 才可填写 `normalized_claim`。
4. `PRESENT`：`out_of_scope=false`；`subtype` 非空；`evidence_span` 与 `normalized_claim` 非空；`explicitness` 必为 `explicit` 或 `implicit`。若含 `procedural`，`procedural_facet` 非空；否则它必须为空。
5. `subtype` 只能含 `distributive` / `procedural`（不重复）；`procedural_facet` 只能含第 6 节五个值（不重复）。
6. `borderline=true` 当且仅当 `confidence="low"`，且 `uncertainty_reason` 非空并只含规定枚举；否则 `uncertainty_reason=""`。
7. 当 `unfair_label="PRESENT"` 时，`evidence_span` 必须逐字来自 review、长度 ≤200 字、且只用 ` […] ` 连接不连续片段（不得出现 `...`/`…`/其他省略符号）。

### 10. 输出前静默自检（不展示、不输出，仅在内部逐条确认后才给出 JSON）

在输出 JSON 前，按下列清单在内部逐条核对；任一条不满足，就先修正再输出，绝不输出不合规的对象：

1. **键完整性：** 恰好 11 个键，拼写完全一致，无多余键，空值用 JSON `null` 而非 `"null"`。
2. **标签依赖：** `out_of_scope`/`unfair_label`/`subtype`/`procedural_facet`/`explicitness`/`borderline`/`uncertainty_reason` 的取值满足第 9 节全部约束。
3. **evidence_span（PRESENT 时最易出错，务必逐字核对）：**
   - 我填的每个片段是否都能在 review 原文里**逐字找到**？（大小写、标点、脏话符号都一致）
   - 总长度是否 ≤200 字？若超了，是否已截成更短的、仍能支撑该判断的子串？
   - 我是否用了 `...`/`…`/换行/破折号来省略或拼接？若是，**必须改成 ` […] ` 或改选更短的单一连续片段**。
4. 若为 `NA`/`ABSENT`，`evidence_span` 是否为 `""`？

---

## 运行前与验证约定（不给模型）

- **目的（这是隔离实验，不是版本升级）：** 保持 v0.3 的**全部格式规则**不变，仅把判据复述次数退回 v0.2 的 1 次，用以判定 v0.2→v0.3 观察到的"英文召回↑ + 硬负例假阳↑"究竟来自**格式纪律**还是来自**判据被反复强化**。**不改动任何判据内容、决策顺序、标签/facet 语义、枚举值或输入边界。**
- **诚实声明：** v0.3 头部"唯一改动 = evidence_span 格式"的自述与实际 diff 不符（判据复述 1→4 次）。该出入已在本文件头部记录，并应记入 `decision_log.md`；v0.3 文件本身不回改（草稿留痕，不原地改写）。
- **不可改变的边界：** 不把人工答案键或当前开发集的个案结论塞入正文；不用 gold 或 stress 修改本 prompt；不以更换模型代替 prompt 校准。
- **待验证（分两步，先小后大）：**
  1. **冒烟（先做）：** 同一 DeepSeek 条件下跑 5 条，含 2026-08-20 曾 `schema_invalid` 的 `103041746`、`103042299`，确认 prompt 抽取成功且格式规则仍生效。
  2. **正式（合格后再做）：** 对**同一份 247 条** `input.jsonl` 跑 v0.4-iso，与已有的 v0.2 / v0.3 逐行配对比较。
---

### v0.4-iso 开发阶段判定协议（2026-08-23 修订版；在看到 v0.4-iso 任何结果之前写死）

> 本节取代本文件初稿中"四种落点"的读法。修订发生在 **v0.4-iso 尚未运行、无任何 v0.4-iso 数据存在**之时，
> 依据是对**已有开发集** v0.2/v0.3 数据做的检出力核算（见下"为什么改"）。它是开发阶段、依赖已有结果的选择协议，**不是独立确认性实验的预注册**。v0.4-iso 跑完后不得再改；如需再改，必须先新增一个版本并记一条 `decision_log.md` 说明理由。

**为什么改（三条实证，全部来自已有数据，未动 gold/stress）：**

1. 配对 McNemar（同 247 行）显示，v0.2→v0.3 的**英文召回差异不显著**：32 条英文人工正例中，
   v0.2 独有命中 1、v0.3 独有命中 4，精确 p = 0.375。原判定规则把它当主轴，是在拿 3 行之差做决策。
2. 唯一接近显著且**单向嵌套**的效应是**严格硬负例假阳**：71 条人工 ABSENT 中，v0.2 独有 0、
   v0.3 独有 5，精确 p = 0.062。v0.3 只增假阳、不减任何一条。这是本样本量下唯一测得动的效应。
3. 「三语 κ 落差 ≤0.15」**不作判据**。① 该阈值出自 `freeze/preregistration.md` §2，测的是
   **Yifan 本人 T1 vs T2 的 intra-rater 漂移**（人 vs 人），且该检验 v1.0 已豁免；预登记中不存在
   模型 vs 人工的 κ 落差条款。② 自助法 95% CI 为 v0.1 [0.07,0.80]、v0.2 [0.06,0.92]、
   v0.3 [0.08,0.96]，三版完全重叠。③ 更根本地，它与抽样设计混淆（见下）。

**主终点（唯一用于取舍的指标）：严格硬负例假阳。**
定义：71 条 `arm=hardneg 且人工=ABSENT` 的行中，模型判 PRESENT 的条数。
检验：与 v0.2、v0.3 分别做**配对 McNemar 精确检验（双侧）**，并**无条件报告不一致格计数 b/c**，
不只报率——b/c 才是配对设计里的信息量所在。

> **选择性披露（须与结果一并报告）：** 该主终点是**在看过 v0.2/v0.3 于这同一批 247 行上的结果之后**
> 选定的——正因为它是本样本里唯一测得动的效应。用同一批数据既选指标又做比较，会使已观察到的
> "v0.2 = 5 / v0.3 = 10" 这一差距**倾向于被高估**（winner's curse）。因此下表的计数档位是
> **本样本内的操作性分界，不是效应量的无偏估计**；任何对外陈述都必须带上这句。
> 另：本样本量下即使出现完全嵌套的最优结果（不一致格 b+c=5），McNemar 精确 p 也只到 0.062，
> **本轮在任何结局下都不可能给出 p<0.05**。本协议据此设计为**描述性决策规则**，不以显著性为冻结条件。

| v0.4-iso 严格假阳 | 判读 | 后续动作 |
|---|---|---|
| 0–5/71，且相对 v0.3 的配对不一致格中“仅 v0.3 为 PRESENT”的条数大于“仅 v0.4-iso 为 PRESENT”的条数 | 结果与“可移除的判据复述增加假阳”一致；但三格设计不能排除交互或 API 抖动 | v0.4-iso 可作为**候选**；冻结前仍须按下方「输出契约门槛」另行写死并版本化 `schema_invalid` 处理规则（**该门槛不是 run 级 pass/fail，也不要求 0 条**——v0.2/v0.3 分别有 10/7 条，若按 0 条读则无一版可冻结），并由 Yifan 决定是否冻结 |
| 10–71/71，且相对 v0.3 的配对不一致格不显示 v0.4-iso 更少假阳 | 结果不支持“删除可移除复述即可恢复 v0.2 假阳水平” | 格式包需重新设计；v0.2 仍是当前较稳候选，但**不因此自动冻结** |
| 6–9/71，或总数与配对方向不一致 | **不确定**；不能按差值分配给两个因素 | 不据此冻结。三格设计无法检验交互，居中或冲突结果也可能是抖动 |

**次要指标（只描述，不用于取舍，且必须与主终点同表呈现）：** 英文 PRESENT 召回、
三语各自召回/假阳/κ、三语 κ 落差及其自助法 CI、subtype/facet 一致率、`schema_invalid_rows`。
**预先声明：本次运行对英文召回没有检出力**（v0.2 vs v0.3 的 p=0.375 即证），
因此**不得**以"v0.4-iso 英文召回更高"作为冻结理由。

**预先写死的停止条件（防止无限调字句）：** 若 v0.4-iso 落入"居中"格，或三语指标仍不可比，
**不得再以"给 prompt 多加一条规则"的方式迭代**。改为回到开发集做**分语言错例归类**
（日文中哪些"像不公、实为普通抱怨"的表达最常被误判），并按下条处理抽样问题。

**已知的、prompt 无法修复的缺陷（须在任何三语结论前解决）：**
诊断臂与硬负例臂中**日文正例 0 条、中文正例 0 条**；32 条英文人工正例全部来自这两个目的性臂，
而 ja(3)/zh(5) 全部来自随机 pilot 臂；在唯一随机且三语等量的 pilot_prompt 臂（45/45/45）内，
**英文人工正例为 0**。因此当前任何"三语对比"都同时在比较**抽样设计**。
要做三语可比，必须先在 **diagnostic / hardneg 开发臂**上补日文与中文正例——
**gold 与 stress 全程不动**。此项独立于 v0.4-iso，两者不互为前置。

---

- **抖动免责：** 已实测 temperature=0 在本条件下不确定（同一行两次跑出 120 字 / 294 字两种 span）。因此 `schema_invalid_rows` ±2 不读作信号，只读标签指标的方向性差异。
- **输出契约门槛（不因抖动豁免）：** `schema_invalid_rows` 可作为校准比较的描述性结果，但任何含原始 `schema_invalid` 输出的 run 都**不得直接充当冻结后生产标签**，也不得手工修补后冒充原始模型输出。若要处理这类行，必须在冻结前另行写死、版本化并验证重试或输出处理规则；该规则也成为完整测量条件的一部分。
- **评分口径前置条件（跑之前必须全部满足）：**
  1. 使用修正后的 `scripts/10_score.py`：严格假阳分母 = 人工 ABSENT；三语各自的召回/假阳/κ；缺失与未解析行计为 `NO_LABEL`、不并入模型真实 NA；κ 落差附自助法 CI 且不打 PASS/FAIL；跨语言表自动打印各语言的臂构成（CONFOUNDED 告警）。
  2. **该脚本已纳入 Git 并记下 commit 与 SHA-256**，与本次比较结果一并存档——决定"哪版胜出"的尺子若不在版本控制里，结论不可复现。
  3. **口径必须在跑之前定死**，否则等于看着结果挑尺子。
- **本次运行不触及：** gold、stress、dev、train。v0.4-iso 是隔离条件，**不因跑完就自动成为最终版本**。
- **冻结条件：** 选定版本后，登记模型提供商和精确 model ID/版本、temperature、top_p、max tokens、seed（若支持）、系统/用户消息拼接方式、输入预处理、解析/重试规则和此文件 SHA-256。随后才可将该完整条件用于 gold 与批量弱标注。

---

### 签署（判定规则生效需本人签字；助手不得代签）

本人已阅读上方「v0.4-iso 开发阶段判定协议（2026-08-23 修订版）」，确认：主终点为严格硬负例假阳、
英文召回仅作描述、三语 κ 落差不作判据、停止条件与抽样缺陷已知悉。

- 签署人：Yifan Mo（`NPC010100Nonbug`）　签署日期：**2026-08-23**
- 签署时**喂给模型的系统指令块**的 SHA-256（第 23 行标题之后至第 159 行标题之前，即实际发往 API 的内容）：
  **`2b10f1b7d03a096abaed97acc77a9786227ada8a69bf5b96e0f25fa0658a0cbe`**（6356 字符）
- 签署时 `annotation_prompt_v0.4_iso_draft.md` **全文** SHA-256（**填入本签署栏之前**的内容）：
  **`e9e722a04c7cc66d930b4b184441b02512ae38ccf370a24432549e778e449e9e`**
- 签署时 `scripts/10_score.py`：Git commit **`73e4c9ea688076214d95322707b3ffa6b0b06afd`**（`73e4c9e`，分支 `main`）／
  SHA-256 **`bcdadcf108dd67799ea609dc194da8a85f5533eceed814f5c314f35b63ca8b10`**

> **关于全文哈希的自指问题（须知悉）：** 把哈希写进文件本身会改变文件哈希，因此上面记录的全文
> 哈希是**填写本栏之前**的内容哈希，签署后的文件哈希必然与之不同。**具有约束力的锚点是第一条
> ——系统指令块哈希**：它是真正发往 API 的内容，且不随本签署栏、头部说明或判定协议的任何后续
> 编辑而改变。核验方式见 `configs/prompts/RUN_GUIDE.md`；亦可用抽取「系统指令」小节后取 SHA-256 复算。
>
> **签署后本判定协议即冻结。** v0.4-iso 跑完后不得修改判定档位、主终点或次要指标的角色；
> 若确需修改，必须新建 v0.5 版本文件并在 `decision_log.md` 记明理由与时间。

> **未签署 = 本判定协议未生效。** 协议未生效就跑，等于跑完再定标准。
> （用词说明：本文件全篇**不使用「预登记」**指称本协议——「预登记」在本仓库专指
> `freeze/preregistration.md` 中的确认性检验。本协议是开发阶段、依赖已有结果的选择协议。）
